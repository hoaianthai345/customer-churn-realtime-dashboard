# Architecture Diagrams

Folder này chứa các tài liệu mô tả luồng hệ thống, contract event, và các thành phần runtime.

Nên đọc theo thứ tự:

1. `architecture.md`
2. `event_contracts.md`

Mục đích:

- vẽ sơ đồ end-to-end cho runtime stack;
- chốt đường đi của data plane và control plane;
- giữ đồng bộ schema event với streaming jobs và ClickHouse sink.
